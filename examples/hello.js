#!/usr/bin/env node
// Simple JavaScript example

const message = "Hello from MockFactory!";
console.log(message);

// Demonstrate some JS features
const numbers = [1, 2, 3, 4, 5];
const sum = numbers.reduce((a, b) => a + b, 0);
console.log(`Sum of ${numbers.join(', ')} = ${sum}`);
